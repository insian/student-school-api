server:
  port: ${PORT:0}
  servlet:
    context-path: /school-app
spring:
  application:
    name: school-app
  datasource:
    url: jdbc:mysql://db-1.cqrf0f1mttmf.us-east-1.rds.amazonaws.com:3306/hr
    username: admin
    password: soham1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  boot:
    admin:
      client:
        url:
        - http://localhost:8888/admin
        instance:
          service-base-url: http://localhost:8888/admin
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    prefer-ip-address: true
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'